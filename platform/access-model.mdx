---
title: Модель доступа
description: Как V3 Custody решает, что вам можно и что вам видно — членство в группах, default-deny, роли на адресах и изоляция видимости.
---

Аутентификация отвечает «кто вы», модель доступа — «что вам можно» и «что вам видно». Складываются три слоя; отдельных «скоупов» у токена нет:

| Слой | Что определяет | Механизм |
|------|----------------|----------|
| членство в группах | какие действия можно | Policy Engine, default-deny |
| гранты на адреса/папки | роли на конкретных адресах | `viewer` / `contributor` |
| видимость | что показывается | изоляция + `visibility-grants` |

<MermaidDiagram chart={`
flowchart TD
  A["1. Кто вы — аутентификация"] --> B["2. Какие действия — группы + Policy Engine (default-deny)"]
  B --> C["3. На каких адресах — роли viewer / contributor"]
  C --> D["4. Что видно — изоляция + visibility-grants"]
  D --> E["5. Какие поля — редакция чувствительных значений"]
`} />

## Права через членство в группах

Действия проходят через Policy Engine, а политики берутся из ваших членств в группах. Права — это объединение allow-политик ваших групп. Работает **default-deny**: без разрешающей политики действие запрещено (`403`).

- `POST /api/v1/policy-groups/{id}/members` — добавить участника в группу.
- `GET /api/v1/me/allowed-segments` — какие значения сегментов вам разрешены для действия.

Подробнее — на странице [Policy Engine](/concepts/policy-engine) и в гайде [Пригласить команду](/guides/invite-team).

## Роли на адресах

Доступ к конкретным адресам задаётся ролью: **`viewer`** (просмотр) или **`contributor`** (просмотр + переводы); право `canCreateAddress` — отдельно. Грант выдаётся на адрес (`/shares`) или на папку (`/grants`), пользователю или группе. Подробнее — в гайде [Выдать и отозвать доступ](/guides/share-access).

## Видимость

По умолчанию видимость изолирована: обычный участник видит только свои адреса, операции и балансы. Права применяются **ещё до агрегации**, поэтому чужие данные не попадают даже в суммарные цифры.

Администратор расширяет видимость точечно через `POST /api/v1/vaults/{code}/visibility-grants` — кто (`subjectType: user|group`) и кого видит (`scope`):

| `scope` | Что видит субъект |
|---------|-------------------|
| `self` | только своё |
| `group` | данные своей группы |
| `vault` | весь Vault |

## Роль администратора

Администратор — **надзорная роль**: видит весь Vault, но сам не инициирует операции (у него нет собственных операций). Это разделяет «того, кто наблюдает» и «того, кто действует».

## Скрытие чувствительных полей

Даже внутри видимых данных значения чувствительных полей редактируются под доступ: значение приходит `null`, а ключ попадает в `redactedFields`. Показывайте «скрыто», а не пустоту — это осознанная редакция, а не отсутствие данных.

## Как всё складывается

1. **Кто вы** — аутентификация.
2. **Какие действия** — членство в группах + Policy Engine (default-deny).
3. **На каких адресах** — роли `viewer` / `contributor` (гранты).
4. **Что видно** — изоляция по умолчанию + `visibility-grants`.
5. **Какие поля** — чувствительные значения редактируются по правам.

<Expandable title="Глоссарий">
  - **default-deny** — без allow-политики действие запрещено.
  - **членство в группах** — источник прав пользователя (объединение allow-политик).
  - **viewer / contributor** — роли на адресах (просмотр / просмотр + переводы).
  - **visibility-grants** — расширение изолированной видимости (`scope`: `self`/`group`/`vault`).
  - **redactedFields** — список полей, чьи значения скрыты под доступ (приходят `null`).
  - **надзорная роль** — администратор: видит весь Vault, не инициирует операции.
</Expandable>

## Частые ошибки

| Симптом | Причина |
|---------|---------|
| `403` у нового пользователя | не добавлен ни в одну группу (default-deny) |
| чужих данных нет даже в суммах | это by design — права применяются до агрегации |
| `null` в поле принят за «пусто» | это редакция: смотрите `redactedFields`, показывайте «скрыто» |
| админ не может отправить перевод | админ — надзорная роль без собственных операций |
| `viewer` пытается перевести | перевод требует роли `contributor` |

## Что дальше

<Columns cols={2}>
  <Card title="Аутентификация детально" icon="key-round" href="/platform/authentication" horizontal>
    Кто вы: сессия, Bearer и cookie.
  </Card>
  <Card title="Policy Engine" icon="shield-check" href="/concepts/policy-engine" horizontal>
    Что можно: политики, группы и default-deny.
  </Card>
  <Card title="Выдать и отозвать доступ" icon="share-2" href="/guides/share-access" horizontal>
    Роли на адресах и папках, аудит доступа.
  </Card>
  <Card title="Контрагенты и контекст" icon="handshake" href="/concepts/counterparties" horizontal>
    Контекст и видимость данных.
  </Card>
</Columns>
